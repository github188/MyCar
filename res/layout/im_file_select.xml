<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/im_chat_log_root"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white"
    android:orientation="vertical" >

    <Button
        android:id="@+id/button_parent_folder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/im_parent_directory" 
        android:background="@drawable/main_button"
        android:textColor="@color/white"
        />

    <ListView
        android:id="@+id/listview_files"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="#00000000"
        android:divider="@drawable/main_divider"
        android:dividerHeight="@dimen/manager_divider_height" >
    </ListView>

</LinearLayout>